name: validate

on:
  pull_request:
    branches:
      - master

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Test branch name
        if: contains(github.head_ref, '.') # Is version branch
        run: exit 1; echo "Cannot PR version branch into master"